# Hook Lab

Install dependencies with `npm install`, add `GEMINI_API_KEY=` from Google AI Studio to `.env.local`, run `npm run dev` locally, then push to GitHub and import the repo into Vercel with the same environment variable configured there.
